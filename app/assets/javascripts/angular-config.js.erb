var app = angular.module("d3playground",['ngRoute', 'ui.bootstrap', 'ngAnimate']);

app.config(['$routeProvider', function($routeProvider){
  $routeProvider.when('/', {
    templateUrl: "<%= asset_path('welcome.html') %>",
    controller: 'RootCtrl'
  })
}]);